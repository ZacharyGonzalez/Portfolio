import './timelineRuler.css'
import Container from '../branch/Container'
export default function TimelneRuler(){
    //each item in the tree should become a node
    return(
        <div className="line-ruler">
            <Container/>
        </div>
    )
}